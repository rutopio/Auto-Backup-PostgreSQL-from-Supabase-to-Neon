# Supabase Database Connection
# Format: postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres?sslmode=require
SUPABASE_DB_URL=postgresql://postgres:your-password@db.xxxxx.supabase.co:5432/postgres?sslmode=require

# Supabase SSL Certificate (base64 encoded)
# Get the certificate from Supabase dashboard and convert to base64:
# - Download the CA certificate file (usually named ca-certificate.crt)
# - Run: cat ca-certificate.crt | base64 | tr -d '\n'
# - Copy the output and paste here
SUPABASE_SSL_CERT_BASE64=2S0tLS1CRUdJTi...your-base64-encoded-certificate...

# Neon Database Connection
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
NEON_DB_URL=postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# Backup Retention
# Number of most recent backup snapshots to keep (default: 7)
# Older snapshots will be automatically deleted
BACKUP_RETENTION_COUNT=7
